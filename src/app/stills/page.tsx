import Portfolio from "@/components/Portfolio";
import { heads } from "@/content";

// Earlier version built from still images + frame interpolation.
export default function Stills() {
  return <Portfolio head={heads.stills} />;
}
